x fix: seeking in audio files
x feat: playback speed controls in audio files
x fix: search bar resizing when typing
x fix: message options toolbar with emojis should be more polished and you shouldn't need to be so precise to use it
x feat: trackpad gesture support in image viewer
x fix: window resizing when zooming in/out in the image viewer
x feat: notifications support
x feat: instead of just always showing the link under the username it should have an option near the username to copy the url or create qr code
x feat: add the ability to see how many messages you have sent in a specific chat, throughout the app, and how many you have received as well per chat and globally in the details page if you enable the option in the settings
x feat: add the ability to send high quality voice messages through the app
x feat: add sound feedback for sent/received messages
x feat: add the ability to enable disappearing messages through the app, and add and improve the general support for them
x fix: make download progress bars actually accurate
x fix: make this actually build on linux as there's an ssl library mismatch

partly  fix: latex rendering being pretty bad and not even rendering fractions
        display maths ($$…$$) is now typeset properly -- a real rule between a
        numerator and a denominator, limits stacked on a sum, a radical with a
        bar, raised scripts, stretched brackets. inline maths ($…$) still uses
        the one-line reading (a⁄b), because a wrapping line has to be a single
        gpui text run and there is no inline box. say the word if you want
        inline to become elements too and lose wrapping inside an equation.

next    the sidebar is still a scrolling div, so it rebuilds every row per
        frame. gpui's `list` would build only what is on screen, the way the
        conversation already does.
